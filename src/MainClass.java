import ComplexNumbers.ComplexNumber;

public class MainClass {
    public static void main(String[] args) {
        ComplexNumber aux = new ComplexNumber(4, 6);
        aux.print();
        
        /* literals 
        float a = 1.0f;
        double b = 1.0d;
        1.0E1       10.0 double
        1E-1d       0.1 double
        1.0e10f     10000000000.0 float 
        java.lang.Float
        java.lang.Double

        Float.NaN       not a number
        Float.NEGATIVE_INFINITY
        Float.POSITIVE_INFINITY 

        if (Math.abs(v1 - v2) < delta 
        
        private static boolean deltaCompare(double v1, double v2, double delta) {
        // return true iff the difference between v1 and v2 is less than delta
            return Math.abs(v1 - v2) < delta;
        } */

        float z_real;
        float z_imaginary;
    }
}
